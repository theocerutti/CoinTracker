import React from 'react';
import {View} from 'react-native';
import {Text} from '../../components';

const Home = (props: any) => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
